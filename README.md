# Web3 Next.js Starter Template (Stable v2)

This is a "Golden Image" template for DeFi applications. It uses pinned versions of Wagmi v2 and RainbowKit v2 to avoid hydration and SSR errors common in Next.js 16/Turbopack.

## 🚀 Quick Start
1. Click **"Use this template"** on GitHub to create a new repo.
2. Clone your new repo and run:
   ```bash
   pnpm install
   pnpm approve-builds
3. Create a .env.local file:
    NEXT_PUBLIC_PROJECT_ID=your_wallet_connect_id_here
4. Start development:
    pnpm run dev